CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT( RayTracer )
FILE( GLOB_RECURSE SRC_FILES *.cpp *.h )

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Ofast -fopenmp -std=c++11")

ADD_EXECUTABLE( RayTracer ${SRC_FILES} )